// src/types/TradeEntry.ts
export interface TradeEntry {
  company: string;
  tradelane: string;
  commodity: string;
  contact: string;
  email: string;
  city: string;
  country: string;
  volume: number;
}